# 部署与版本说明

> 本文记录正式部署机的系统信息，由部署维护者更新；信息变化时同步修改本文件。
> 最近更新：2026-09-03

## 基础项目

- 项目：Open WebUI（开源 AI 对话与工具平台）
- 版本：v0.11.0
- 官方仓库：https://github.com/open-webui/open-webui
- 官方文档：https://docs.openwebui.com

## 正式部署机（当前即仓库所在本机）

- 项目目录：`E:\Code\codex\course-ai-assistant`
- Open WebUI 源码：`open-webui\`（v0.11.0 快照）
- 运行时数据：`open-webui\local-deploy\data\`（webui.db、uploads、vector_db、cache）——**不入库**
- 访问地址：本机 `http://localhost:8080`；组员共享地址待开放后填写
- 启动方式（示例）：
  1. 设置 `DATA_DIR` 为 `open-webui\local-deploy\data`；
  2. 设置 `FRONTEND_BUILD_DIR` 为仓库 `build` 目录（不设置会“Serving API only”导致 404）；
  3. 设置 `WEBUI_SECRET_KEY`（读取 `local-deploy\webui_secret_key.txt`，该文件不入库）；
  4. 运行 `open-webui\local-deploy\venv312\Scripts\open-webui.exe serve --host 0.0.0.0 --port 8080`。

## 模型接入

- 对话模型：DeepSeek（OpenAI 兼容方式）
  - API Base URL：`https://api.deepseek.com`
  - 当前可用/使用模型：`deepseek-v4-flash`（deepseek-chat 系列）
  - API Key：仅在本机 Open WebUI 管理界面（设置 → 外部连接）中配置，**不写入仓库、不写入本文档**
- 备用连接：OpenAI 兼容入口已配置但本仓库不记录密钥；
- Ollama：配置保留但本机当前未安装运行 Ollama。

## RAG / 知识库

- 嵌入引擎：Default（SentenceTransformers，本地运行，不消耗 API token）
- 嵌入模型：`sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2`（中文检索；首次使用自动下载到本机 HuggingFace 缓存）
- 分块与检索参数：chunk_size=1000、chunk_overlap=100、top_k=3；已关闭“PDF 提取图片”（避免 PPT 转 PDF 中异常图片导致解析崩溃）
- 向量库：Chroma，位于 `open-webui\local-deploy\data\vector_db`
- 上传文件：`open-webui\local-deploy\data\uploads`
- 元数据/状态：`open-webui\local-deploy\data\webui.db`
- 知识库现状（2026-09-03）：工作区知识库“Python程序设计”，169 个文件处理完成并关联（讲义、习题题目/答案 PDF、教学大纲、示例代码等）；该库向量集合约 1.5 万条向量
- 重建方式：克隆仓库 → 启动 Open WebUI → 在工作区上传 `knowledge-base/` 目录 → 系统自动切分并向量化；向量库与上传文件无需也不应进入 Git

## 更新流程

1. 组员将代码/文档改动推送到本仓库；
2. 部署维护者在部署机拉取代码；
3. 按改动类型重启 Open WebUI 或重新加载；
4. 全组通过共享地址验证。

## 版本记录

| 日期 | 版本/变更 | 说明 |
|---|---|---|
| 2026-09-02 | v0.11.0 | 初始化记录，正式对外访问地址待填写 |
| 2026-09-03 | v0.11.0 | 接入 DeepSeek 对话模型；配置本地多语言嵌入模型；修复前端 build 404 与 PDF 图片提取崩溃；知识库“Python程序设计”入库 169 个文件；创建专属模型“Python程序设计基础 AI 助教” |
