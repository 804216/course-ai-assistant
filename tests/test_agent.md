# Python 课程 AI 助教测试报告

- 测试模型：`python_course_langgraph`（Pipe 1.5.0）
- 知识库构建：`20260904-v3`
- 测试方式：通过 Open WebUI `/api/chat/completions` 接口逐题真实调用，非模拟输出
- 测试规模：15 题（课程知识问答 5、综合分析 3、知识库无答案 2、练习生成或批改 2、自定义工具 2、错误输入 1）

## 总体结果

| 类别           | 数量 | 结果摘要                                                                       |
| -------------- | ---: | ------------------------------------------------------------------------------ |
| 课程知识问答   |    5 | 5 题均召回相关章节并给出来源；参数传递术语仍可更严谨                           |
| 综合分析       |    3 | 异常流程分析正确；容器比较存在现代 Python 版本语义问题；综合作业提示给得过完整 |
| 知识库无答案   |    2 | 均明确说明无法确认，没有编造具体答案                                           |
| 练习生成或批改 |    2 | 按章出题、只给提示；批改覆盖普通、空列表和非数字输入                           |
| 自定义工具     |    2 | 章节目录和第 6 章代码统计均与目录数据库一致                                    |
| 错误输入       |    1 | 第 16 章在模型调用前被拒绝                                                     |

回答正确性统计：12 题正确，2 题基本正确，1 题部分正确。引用方面没有发现伪造文件；第 12 题的来源只直接支持平均值计算，不能完整支撑异常行为分析，已标记为“部分充分”。

## 逐题记录

### T01 课程知识问答：Python 全部关键字

**测试输入**

> Python语言中所有关键字有哪些？请只依据课程资料完整列出并给出来源，控制在350字以内。

**AI 实际输出**

> 根据课程讲义表2-1，Python关键字如下：and、as、assert、async、await、break、class、continue、def、del、elif、else、except、finally、for、from、False、global、if、import、in、is、lambda、nonlocal、not、None、or、pass、raise、return、try、True、while、with、yield[资料1]。
>
> 关键字是Python语言中定义的一些专有词汇，具有特定的含义，只能用于特定的位置。Python关键字区分字母大小写，例如`for`是关键字，而`FOR`不属于关键字。资料还给出了查看方法：`import keyword; keyword.kwlist`[资料1]。
>
> 需注意：讲义正文举例时提到过`print`，但表2-1中并未列出`print`，因此关键字列表以表2-1为准[资料1]。
>
> 来源：《第02章 基础语法知识》讲义，2.1.1节，第3-6页，表2-1[资料1]。
>
> 资料来源：
>
> - 第02章-基础语法知识-讲义.pdf，第3-9页

- 是否引用正确资料：是。引用第 2 章关键字表，文件和页码与检索结果一致。
- 回答是否正确：是。完整列出课程资料中的 35 个关键字，并正确排除 `print`。
- 存在的问题：用户要求 350 字以内，回答略长；正文页码“3-6页”和自动来源汇总“3-9页”粒度不一致。
- 改进方式：为“完整列表”问题采用紧凑表格；来源统一使用实际知识块页码范围。

### T02 课程知识问答：函数参数传递

**测试输入**

> 第6章中函数参数传递有哪些主要方式？请依据课程资料简要解释并给出来源，控制在300字以内。

**AI 实际输出**

> 结论：第6章函数参数传递主要按“实参对象是否可变”区分传递方式；此外还涉及位置参数、关键字参数、默认参数、不定长参数等多种参数类型。
>
> 解释：
>
> - 传递不可变对象（如数字、字符串、元组）：只传递值，函数内修改形参不会影响外部实参[资料3][资料1]。
> - 传递可变对象（如列表、字典）：传递引用，函数内修改形参会同步影响外部实参[资料1][资料3]。
> - 参数类型方面：位置参数调用时顺序和数量需一致；关键字参数按参数名传值，顺序可调；默认参数定义时给定默认值；不定长参数用`*parameter`接收元组或`**parameter`接收字典[资料4][资料5]。
> - 还可以用`*`、`**`对序列/字典解包传递参数；用`/`和`*`限定仅限位置参数或仅限关键字参数[资料2]。
>
> 来源：第06章-函数-讲义.pdf第31-34、38-41、43-49、53-58页[资料1][资料2][资料3][资料4][资料5]。
>
> 资料来源：
>
> - 第06章-函数-讲义.pdf，第38-41页
> - 第06章-函数-讲义.pdf，第53-58页
> - 第06章-函数-讲义.pdf，第31-34页
> - 第06章-函数-讲义.pdf，第43-45页
> - 第06章-函数-讲义.pdf，第48-49页

- 是否引用正确资料：是。所有来源均来自第 6 章函数讲义。
- 回答是否正确：基本正确。参数类型、解包和限定方式说明正确。
- 存在的问题：“不可变对象只传递值、可变对象传递引用”是教材式简化，容易让学生误以为 Python 同时采用两套传参机制；更准确的说法是对象共享/对象引用传递，差异来自对象是否可变。
- 改进方式：先忠实说明教材表述，再增加一句现代 Python 语义提示；压缩来源列表以满足 300 字要求。

### T03 课程知识问答：列表与元组

**测试输入**

> Python列表与元组最核心的区别是什么？请给一个简短例子并引用课程资料，控制在300字以内。

**AI 实际输出**

> 结论：列表和元组最核心的区别是：**列表可变，元组不可变**。
>
> 解释：列表可以随时添加、修改或删除元素；元组一旦创建就不能改变，没有 `append()`、`remove()` 等方法，也不能删除其中元素。如果只是遍历数据而不需要修改，用元组更合适。
>
> 示例：
>
> ```python
> lst = [1, 2]
> lst.append(3)      # 列表可修改，正常执行
>
> tup = (1, 2)
> # tup.append(3)    # 元组没有 append()，会报错
> ```
>
> 来源：[资料1] 第04章序列讲义第56页明确说明：“列表属于可变序列……元组属于不可变序列，没有append()、extend()和insert()等方法，不能修改其中的元素”[资料1]。另外，正因元组不可变，它可作为字典的键，而列表不可以[资料1][资料3]。
>
> 资料来源：
>
> - 第04章-序列-讲义.pdf，第54-58页
> - 第04章-序列-讲义.pdf，第61-65页

- 是否引用正确资料：是。引用第 4 章序列讲义。
- 回答是否正确：是。
- 存在的问题：示例将会报错的代码注释掉，学生无法直接观察异常类型；回答也略超字数限制。
- 改进方式：明确写出预期 `AttributeError`，并减少重复来源描述。

### T04 课程知识问答：pickle 序列化

**测试输入**

> pickle.dumps和pickle.loads如何配合完成序列化与反序列化？请依据课程资料回答，控制在300字以内。

**AI 实际输出**

> 结论：`pickle.dumps` 负责“序列化”，把 Python 对象变成字节流；`pickle.loads` 负责“反序列化”，把字节流还原成原对象。两者配合时，`loads` 的参数就是 `dumps` 的返回值。
>
> 解释：按课程讲义，先 `import pickle`，用 `pickle.dumps(vCard)` 得到类似 `b'\\x80\\x04...'` 的字节串，肉眼不可直接读取[资料1][资料2]。之后用 `pickle.loads(该字节串)`，即可恢复出原来的字典对象[资料1]。
>
> 示例（Python 3，需导入 pickle）：
>
> ```python
> import pickle
> vCard = {'firstName': '名', 'lastName': '姓'}
> data = pickle.dumps(vCard)  # data 是 bytes
> obj = pickle.loads(data)    # 还原为原字典
> ```
>
> 需要注意：pickle 序列化结果是“字节流”，不是可读字符串，而且**不安全**。课程资料强调：不要反序列化不信任来源的数据，否则恶意构造的字节流可能在反序列化时执行恶意代码；必要时应先用 MD5/SHA-1 签名验证再反序列化[资料1]。
>
> 来源：第10章讲义第18—21页[资料1]；第13—18页[资料2]。
>
> 自我检查：检查时确认传给 `loads` 的字节串确实是由 `dumps` 得到的，且来源可信。
>
> 资料来源：
>
> - 第10章-基于文件的持久化-讲义.pdf，第19-21页
> - 第10章-基于文件的持久化-讲义.pdf，第13-18页

- 是否引用正确资料：是。第 10 章来源与 API 流程匹配。
- 回答是否正确：是，并包含反序列化安全提醒。
- 存在的问题：明显超过 300 字；“MD5/SHA-1 签名验证”容易被理解为推荐现代安全方案，措辞需谨慎。
- 改进方式：主回答只保留 `dumps → bytes → loads`；安全扩展压缩为“仅反序列化可信数据”，避免展开过时算法。

### T05 课程知识问答：break 与 continue

**测试输入**

> for循环中的break与continue有什么区别？请结合课程资料和最小示例回答，控制在350字以内。

**AI 实际输出**

> 结论：**break 用于“终止整个循环”，continue 用于“跳过这一次，继续下一次”。**
>
> **解释或步骤**把循环想象成排队领东西：
>
> - **break**：遇到它，你直接离开队伍，后面的人都不管了。
> - **continue**：遇到它，你暂时出队，但马上回到队尾，等待下一轮。
>   所以写循环时，想“提前结束”用 break；想“跳过某些值”用 continue。
>
> **正式定义**
> 课程讲义指出：break 会“强行终止循环”，即使循环条件未达到 False，也会停止[资料2]；continue 则“跳出本次循环”，即跳过当前剩余语句，继续执行下一轮循环[资料1]。
>
> **最小示例**（Python 3）
>
> ```python
> for i in range(5):
>     if i == 3:
>         continue
>     print(i)
> # 输出：0 1 2 4
>
> for i in range(5):
>     if i == 3:
>         break
>     print(i)
> # 输出：0 1 2
> ```
>
> **来源**
>
> - [资料1]：第03章-程序控制结构-讲义.pdf，第42-47页（含例3-20）
> - [资料2]：第03章-程序控制结构-讲义.pdf，第35-38页（含例3-16）
>
> **自我检查**
> 试着运行上面两段代码，观察 i=3 时输出有何不同，即可加深记忆。

- 是否引用正确资料：是。引用第 3 章相应控制语句内容。
- 回答是否正确：是。
- 存在的问题：类比中“continue 回到队尾”不够严谨，容易误解为数据顺序改变；输出注释放在一行，与真实 `print` 逐行输出不完全一致。
- 改进方式：改为“跳过本轮剩余步骤，开始下一轮”；输出使用多行文本展示。

### T06 综合分析：列表、元组和字典

**测试输入**

> 综合比较列表、元组和字典在可变性、访问方式与适用场景上的差异，并给出选择建议。只依据课程资料，控制在500字以内。

**AI 实际输出**

> 结论：列表是可变的有序序列，元组是不可变的有序序列，字典是可变的无序“键-值”映射集合。三者在可变性、访问方式和适用场景上有明显不同。
>
> 解释：
>
> - **可变性**：列表属于可变序列，可以用 append()、extend()、insert() 添加元素，用 del、remove()、pop() 删除元素，也支持用切片修改元素 [资料1]。元组属于不可变序列，不能增删改元素，也没有相关方法 [资料1]。字典是可变序列，支持通过 dictname[key]=value 添加或修改，用 del 删除键值对 [资料5]。字典的键必须不可变且唯一，因此列表不能作键，元组可以 [资料1][资料2]。
> - **访问方式**：列表和元组都支持索引和切片访问，但列表能用切片修改元素，元组不能 [资料1]。字典不支持索引和切片，只能通过“键”来访问对应的值，也可用 get()、keys()、values()、items() 等方法操作 [资料2][资料3]。
> - **适用场景**：列表适合需要频繁增删改且保持顺序的数据；元组适合只需遍历、保护数据不被修改，或作为字典键使用；字典适合保存具有映射关系的数据，如课程成绩与课程名称的对应 [资料2][资料5]。
>
> 选择建议：需要按位置修改和存储时选列表；数据只读、需要更快访问速度或作为键时选元组；需要按某个键快速查找值时选字典 [资料1][资料2]。
>
> 来源：[资料1][资料2][资料3][资料5]
> 自我检查：可根据实际场景判断——待处理数据是否要修改、是否要按键查找、是否需要保持顺序，再选择合适容器。
>
> 资料来源：
>
> - 第04章-序列-讲义.pdf，第54-58页
> - 第04章-序列-讲义.pdf，第61-65页
> - 第04章-序列-讲义.pdf，第66-71页
> - 第04章-序列-讲义.pdf，第75-81页

- 是否引用正确资料：是。来源均为第 4 章相关知识块。
- 回答是否正确：部分正确。列表、元组的可变性及三者访问方式基本正确。
- 存在的问题：称字典为“无序”符合旧教材表述，但 Python 3.7+ 的语言规范保证字典保持插入顺序；“元组访问更快”也不应作为没有条件说明的选型依据。
- 改进方式：提示“课程资料采用无序映射表述；现代 Python 保持插入顺序，但仍应通过键访问”；选型建议以语义和可变性为主。

### T07 综合分析：异常处理流程

**测试输入**

> 分析下面代码的异常处理流程：
>
> ```python
> try:
>     x = int(input())
>     print(10 / x)
> except ValueError:
>     print('输入错误')
> except ZeroDivisionError:
>     print('不能为0')
> finally:
>     print('结束')
> ```
>
> 请说明三种输入下的执行结果并引用课程资料，控制在500字以内。

**AI 实际输出**

> 结论：该代码使用 `try-except-finally` 结构处理异常。`except` 分支只捕获对应类型的异常；无论是否发生异常、是否被捕获，`finally` 分支都会执行。下面按三种输入说明执行结果。
>
> **三种输入的执行结果**
>
> 1. 输入非数字（如 `abc`）：`int(input())` 抛出 `ValueError`，打印 `输入错误`，随后打印 `结束`。
> 2. 输入 `0`：`10 / 0` 抛出 `ZeroDivisionError`，打印 `不能为0`，随后打印 `结束`。
> 3. 输入非零整数（如 `2`）：打印 `5.0`，不进入 `except`，最后打印 `结束`。
>
> **解释**
> `try` 中抛出异常后按顺序匹配 `except` 类型；匹配成功执行对应分支，否则异常继续向上传递 [资料1]。`finally` 无论是否捕获异常都执行，与 `exception05.py`、`exception06.py` 示例一致 [资料2][资料3]。
>
> **来源**
> [资料1] 第09章-异常处理/示例代码/exception07.py，第1-19行
> [资料2] 第09章-异常处理/示例代码/exception05.py，第1-10行
> [资料3] 第09章-异常处理/示例代码/exception06.py，第1-12行
>
> **自我检查**
> 可在本地 Python 3 环境分别输入上述三类值验证输出顺序；`10 / 2` 的结果是浮点数 `5.0`。

- 是否引用正确资料：是。三个代码文件均真实存在于第 9 章，行号范围有效。
- 回答是否正确：是。三条控制流和输出均正确。
- 存在的问题：实际回答超过 500 字，且额外解释了本代码没有使用的 `else` 分支。
- 改进方式：优先用三行表格表示输入、异常、输出，仅保留与当前代码直接相关的内容。

### T08 综合分析：文件词频统计方案

**测试输入**

> 设计一个读取文本文件、统计单词频次并写入结果文件的实现思路，综合使用函数、字典、文件与异常处理。不要给可直接提交的完整作业，只给步骤、伪代码和来源，控制在500字以内。

**AI 实际输出**

> 结论：把任务拆成“读文件、统计词频、写结果”三个独立函数，用字典存放频次，用 try/except 捕获异常，用 with open 管理文件。
>
> **步骤或思路**
>
> 1. 读文件函数：以 `"r"` 模式打开文本，读取全部内容。
> 2. 统计函数：对每个词执行 `counts[word] = counts.get(word, 0) + 1`。中文分词可参考 `jieba.cut`。
> 3. 写文件函数：以 `"w"` 模式打开结果文件，逐行写出单词和次数。
> 4. 主流程：在 `try` 中依次调用，`except` 捕获 `FileNotFoundError` 等异常。
>
> **伪代码（仅示意，不可直接提交）**
>
> ```python
> def read_file(path):
>     with open(path, "r", encoding="utf-8") as f:
>         return f.read()
>
> def count_words(text):
>     counts = {}
>     for w in text.split():
>         if len(w) == 1:
>             continue
>         counts[w] = counts.get(w, 0) + 1
>     return counts
>
> def write_result(counts, out):
>     with open(out, "w", encoding="utf-8") as f:
>         for w, c in counts.items():
>             f.write(f"{w}\\t{c}\\n")
>
> def main():
>     try:
>         text = read_file("in.txt")
>         counts = count_words(text)
>         write_result(counts, "out.txt")
>     except Exception as e:
>         print("Error!", e)
> ```
>
> 注意：不要让 `read_file` 在捕获异常后直接返回 `None`，最好在 `main` 中统一捕获或重新抛出。
>
> **来源**
>
> - 字典统计逻辑：`wordcount.py`，第15章，[资料1]
> - 异常处理结构：`exception03.py`、`exception07.py`，第9章，[资料2][资料3]
> - with open 写文件：`parse_poem.py`，第14章，[资料6]
> - 函数定义与返回值：`sum_seq.py`，第6章，[资料8]
>
> **自我检查**：注意路径与编码；考虑空文件、大小写、标点；若用 `jieba` 需先安装依赖。

- 是否引用正确资料：基本正确。列出的 `wordcount.py`、异常示例、`parse_poem.py` 和 `sum_seq.py` 都真实存在且主题相关。
- 回答是否正确：基本正确。模块拆分与字典累计思路合理。
- 存在的问题：虽然标为伪代码，但内容已经接近可直接运行的完整作业，违反用户“只给步骤、伪代码”的限制；`except Exception` 捕获过宽；`text.split()` 不能正确完成一般中文分词；回答超过 500 字。
- 改进方式：伪代码只写函数职责和关键表达式，留出核心循环让学生完成；仅提示捕获 `FileNotFoundError`、`UnicodeError`、`OSError`；明确英文分词与中文分词的差异。

### T09 知识库无答案：CPython 3.13 自由线程

**测试输入**

> 课程资料是否说明了CPython 3.13自由线程构建中GIL的底层实现细节？只依据知识库回答；资料没有时请明确说明。

**AI 实际输出**

> 结论：根据当前提供的课程资料，**无法确认**是否有关于“CPython 3.13 自由线程构建中 GIL 的底层实现细节”的说明。
>
> 解释或步骤：本次检索获得的资料主要是程序控制结构、函数习题和 GUI 示例，均没有涉及 CPython 解释器内部实现、GIL 或自由线程构建。因此，基于所给资料无法回答该问题，也不能推测或编造相关资料。
>
> 来源：所给资料中未包含与 CPython 3.13、自由线程构建、GIL 底层实现相关的说明，故无对应资料可引用。
>
> 自我检查或下一步：如果问题来自扩展阅读，建议咨询任课教师或查阅 CPython 官方文档；该问题属于解释器实现层面，超出本课程常规范围。

- 是否引用正确资料：适用。知识库没有直接来源，回答明确说明无对应资料，未伪造引用。
- 回答是否正确：是。
- 存在的问题：回答列举了若干无关召回章节，会增加噪声；“无法确认是否有说明”不如“本次知识库检索未找到相关说明”直接。
- 改进方式：无答案分支只报告检索结论，不展示无关候选资料；统一使用“资料中未找到”。

### T10 知识库无答案：2027 年期末考试

**测试输入**

> 课程资料中是否记录了本课程2027年期末考试的具体题目和标准答案？只依据知识库回答；资料没有时请明确说明。

**AI 实际输出**

> 结论：根据知识库检索结果，无法确认课程资料中是否记录了2027年期末考试的具体题目和标准答案；现有结构化资料中没有出现“2027年期末考试”或“标准答案”的相关条目。
>
> 解释或步骤：本次返回的知识库结构化结果仅包含课程章节的章次、章名、文件数、核心知识块数量和解法知识块数量（build_id: 20260904-v3），没有提供任何关于2027年期末考试具体题目或标准答案的文件名、页码、题号或内容。因此，无法依据现有资料确认该类信息，也不能推断知识库中是否存在未列出的相关内容。
>
> 来源：[知识库结构化工具结果，build_id: 20260904-v3；仅含第0—15章章节统计，未见2027年期末考试相关信息]
>
> 自我检查或下一步：如需复习，可以指定章节或知识点生成自测练习；未绑定的参考答案库无法读取或确认。

- 是否引用正确资料：是。引用的是实际课程目录工具结果，并明确其只能证明当前可见内容中未找到。
- 回答是否正确：是，没有编造考试题目或答案。
- 存在的问题：意图被路由到课程目录统计，输出了与问题不直接相关的章节计数背景；“无法推断是否存在未列出的内容”稍显冗长。
- 改进方式：对“是否存在某具体文件/考试资料”增加目录精确搜索能力，直接返回匹配数量和“不存在/无权访问”的区别。

### T11 练习生成：第 6 章两道初学者题

**测试输入**

> 请根据第6章资料生成2道初学者练习题，分别标明知识点和难度，每题只给一条提示，不要给完整答案，并注明资料来源。

**AI 实际输出**

> 根据第6章“函数”章节材料，为你生成两道面向初学者的练习题。题目在知识范围内进行了改编，不逐字照抄原题，也不提供完整答案。
>
> **练习 1**
>
> - 章节：第6章 函数
> - 知识点：函数定义与返回值、循环累加、数列生成
> - 难度：初学者
> - 题目：编写函数 `fib_sum(n)`，接收正整数 `n`，返回斐波那契数列前 `n` 项的和。
> - 作答要求：只写函数定义和函数体；不要使用递归；确保 `n=1`、`n=2` 时正确。
> - 提示：始终记住前两个数的状态，每算出一个新数就加入累计总和。
> - 来源：改编自《第06章-函数-题目.pdf》第2页教材第6章习题第1题。
>
> **练习 2**
>
> - 章节：第6章 函数
> - 知识点：函数参数、循环嵌套、`print` 输出控制
> - 难度：初学者
> - 题目：编写函数 `print_half_pyramid(n)`，输出共 `n` 行的右对齐直角三角形。
> - 作答要求：不要使用 `center`、`ljust`、`rjust`，使用循环和空格拼接。
> - 提示：每行先输出 `n-i` 个空格，再输出 `i` 个星号。
> - 来源：改编自《第06章-函数-题目.pdf》第2页教材第6章习题第2题。
>
> 两道题均未给出完整答案。完成后可提交尝试代码继续分析。
>
> 资料来源：
>
> - 第06章-函数-题目.pdf

- 是否引用正确资料：是。练习工具返回的真实来源为第 6 章题目文件。
- 回答是否正确：是。数量、章节、知识点、难度和单条提示均符合要求，没有给完整代码答案。
- 存在的问题：第一题和原题相似度仍较高；第二题措辞出现“不要调用：不要使用”的重复。
- 改进方式：增加题型变换或参数变化，降低与原题的表面重合；生成后做语句去重检查。

### T12 答案批改：列表平均值

**测试输入**

> 请分析我的答案。题目：编写函数计算列表平均值。我的代码是：
>
> ```python
> def average(nums):
>     total = 0
>     for n in nums:
>         total += n
>     return total / len(nums)
> ```
>
> 请分析普通输入、空列表和非数字元素，并给出改进建议，不要重写成整份作业。

**AI 实际输出**

> **正确之处**
>
> - 函数使用 `def average(nums):`，带参数并有 `return`，结构完整；第 6 章函数示例同样使用 `def` 和 `return` [资料4]。
> - 对普通数值列表，如 `[1, 2, 3, 4]`，循环累加后除以 `len(nums)`，能正确返回 `2.5`，代码没有语法错误。
>
> **存在问题**
>
> - 空列表：`len([])==0`，最后一行会触发 `ZeroDivisionError`。
> - 非数字元素：如 `[1, 'a']`，执行 `total += n` 时会因 `int` 与 `str` 不能相加而抛出 `TypeError`。
> - 若传入 `None`，在迭代或调用 `len` 时也会报错；`bool` 是 `int` 的子类，是否允许参与平均值取决于题目要求。
>
> **改进建议**
>
> - 在函数开头判断空列表，可返回 `None` 或抛出 `ValueError("空列表不能计算平均值")`。
> - 累加前可使用 `isinstance(n, (int, float))` 检查元素；发现非数字时抛出 `TypeError`。
> - 如果题目已保证元素均为数字，空列表保护是主要改进点，不必过度设计。
>
> **自我检查**
>
> - 分别运行 `average([1,2,3,4])`、`average([])`、`average([1,'a'])`。
> - 思考空列表返回 `0` 是否会与“平均值为 0”混淆。
> - 思考类型检查应放在循环内还是循环外。
>
> 补充说明：知识库没有针对空列表和非数字元素处理的直接讲解，因此错误行为部分基于 Python 基础运行机制，无法从课程资料中直接确认。
>
> 资料来源：
>
> - variance.py，第1-20行

- 是否引用正确资料：部分充分。`variance.py` 与数值统计和函数结构相关，但不能直接支持 `ZeroDivisionError`、`TypeError` 等全部分析；回答已明确区分这一点。
- 回答是否正确：是。三种输入的行为分析正确，且没有重写完整作业。
- 存在的问题：来源与核心错误分析的对应度不高；给出的局部代码已非常接近修复答案；对 `None`、`bool` 的扩展超出题目重点。
- 改进方式：优先检索第 9 章异常类型作为补充证据；第一轮反馈只给判断条件和异常类型，由学生自己补写代码。

### T13 自定义工具：课程章节目录

**测试输入**

> 本课程有哪些章节？请调用课程目录能力，只列出章节编号和名称，不要凭记忆补充。

**AI 实际输出**

> 结论：本课程《Python程序设计基础教程（微课版）》的章节如下。
>
> - 0　课程说明
> - 1　Python语言概述
> - 2　基础语法知识
> - 3　程序控制结构
> - 4　序列
> - 5　字符串
> - 6　函数
> - 7　面向对象程序设计
> - 8　模块
> - 9　异常处理
> - 10　基于文件的持久化
> - 11　基于数据库的持久化
> - 12　图形用户界面编程
> - 13　正则表达式
> - 14　网络爬虫
> - 15　常用的标准库和第三方库
>
> 来源：检索到的课程目录结构化结果（编号0—15）。

- 是否引用正确资料：是。来自 `get_course_outline` 的活动目录数据。
- 回答是否正确：是，章节编号和名称与目录数据库完全一致。
- 存在的问题：用户要求“只列出”，回答仍增加了结论、来源和下一步。
- 改进方式：对“只列出/只报告”指令启用严格简洁输出模板。

### T14 自定义工具：第 6 章代码资料

**测试输入**

> 第6章有哪些示例代码资料？代码类型共有多少个知识块？请调用章节资料查询能力并报告真实结果。

**AI 实际输出**

> 结论：第6章“函数”共有 **17 个示例代码资料**，资源类型均为 `code`，状态全部为 `completed`；代码类型知识块数量为 **17 个**。
>
> 资料文件：
>
> 1. `anno_demo.py`
> 2. `docstr.py`
> 3. `factorial_loop.py`
> 4. `factorial_recursion.py`
> 5. `fibonacci.py`
> 6. `func_var1.py`
> 7. `func_var2.py`
> 8. `func_var3.py`
> 9. `i_like.py`
> 10. `param1.py`
> 11. `param2.py`
> 12. `param3.py`
> 13. `quotient.py`
> 14. `sum_seq.py`
> 15. `transfer_immutable.py`
> 16. `transfer_mutable.py`
> 17. `variance.py`
>
> 来源：章节资料查询工具返回 `chapter_no: 6`、`chapter_title: 函数`、`resource_type: code`、`chunk_counts.code: 17` 和上述 17 条材料记录。

- 是否引用正确资料：是。来自 `get_chapter_materials(6, "code")` 的真实结构化结果。
- 回答是否正确：是。文件数和知识块数均为 17，与目录数据库一致。
- 存在的问题：“17 个示例代码资料”和“17 个知识块”数值相同，容易让学生误以为所有代码永远都只切成一个知识块。
- 改进方式：明确区分“17 个文件，本次构建恰好对应 17 个代码知识块”。

### T15 错误输入：不存在的第 16 章

**测试输入**

> 请生成第16章的3道练习题。

**AI 实际输出**

> 课程章节编号必须是1到15。

- 是否引用正确资料：不需要引用。该请求在输入校验阶段终止。
- 回答是否正确：是，响应耗时约 0.04 秒，没有调用知识库或基础模型。
- 存在的问题：只说明合法范围，没有进一步提示可查询课程目录。
- 改进方式：补充“可先询问课程有哪些章节”，但保持错误响应简洁。

## 优化前后效果对比

### 对比一：完整关键字表召回

| 项目     | 优化前                                                                             | 优化后                                                                                 |
| -------- | ---------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| 测试问题 | Python 中所有的关键字                                                              | 与 T01 相同                                                                            |
| 实际结果 | “结论：根据现有课程资料，无法确认 Python 中所有关键字的完整列表。”               | 完整列出 35 个关键字，引用第 2 章讲义表 2-1，并说明`print` 不在表中                  |
| 是否正确 | 否。资料中存在完整表格但未召回                                                     | 是                                                                                     |
| 原因     | 仅依靠语义向量相似度时，“所有关键字”表格块没有稳定排在前列；表格和相邻说明被分割 | 增加 BM25 精确短语、向量与 BM25 的加权 RRF、BGE 重排、完整清单锚点保护和相邻知识块合并 |
| 效果     | 错误进入“资料不足”分支                                                           | 稳定命中第 2 章表格并给出真实页码                                                      |

### 对比二：明显课程越界问题

| 项目     | 优化前                                                                       | 优化后                                                                                                                 |
| -------- | ---------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| 测试问题 | 请介绍今天的足球比赛结果                                                     | 相同                                                                                                                   |
| 实际结果 | 最终虽然说明超出课程范围，但先检索了数据库、SQL 等无关课程资料并将其写入解释 | 直接返回“这超出了《Python程序设计基础》课程范围……”                                                                 |
| 问题     | 分类模型偶尔把明显越界问题路由为知识问答，造成无效检索和无关引用             | 在模型分类前增加今日体育、新闻、天气、股价等明显实时问题的确定性越界识别；包含“Python、代码、爬虫”等课程语境时不拦截 |
| 效果     | 响应较慢且存在无关来源噪声                                                   | 不调用检索和生成，快速、稳定、无伪引用                                                                                 |

## 后续优化优先级

1. 增加课程版本事实修正层：教材与现代 Python 行为不一致时，同时给出“教材口径”和“当前版本口径”。
2. 将长度要求、严格列表格式和答案点评四段式加入输出校验，而不只依赖提示词。
3. 对作业型综合任务降低代码生成完整度：首轮只给步骤、接口和关键提示，收到学生尝试后再逐步展开。
4. 增加“目录精确存在性查询”，区分资料不存在、答案集合受控、当前用户无权访问三种状态。
5. 对无答案问题过滤无关候选，避免在拒答说明中罗列不相干章节。
